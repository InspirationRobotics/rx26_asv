<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>crusader_link</name>
  <version>0.1.0</version>
  <description>
    The boat's end of the UAV link. rxl_link_node decodes the RXL dialect off a
    radio and republishes it as ROS messages, and sends back the one message the
    boat transmits.

    It speaks MAVLink but it is NOT a second autopilot gateway: a different
    endpoint, a different dialect, and a message set with no command in it. It
    cannot arm, set a mode or move the boat. crusader_fcu/telemetry_bridge
    remains the only path to the Pixhawk, and the two never share a socket.

    Version 0.1.0: the version counts capability proven, and nothing here has
    carried a frame over a real radio yet -- only UDP loopback against the
    bench.
  </description>
  <maintainer email="brandont3927@gmail.com">Team Inspiration</maintainer>
  <license>MIT</license>
  <url type="repository">https://github.com/InspirationRobotics/rx26_asv</url>

  <depend>rclpy</depend>
  <depend>std_msgs</depend>
  <depend>crusader_msgs</depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
